let M := 24;
let N := 4302;
let nzeros := 1;
let sigma := 0.5;
read {j in 1..N} Y[j] < robot_label.txt;
read {i in 1..M, j in 1..N} X[i,j] < robot_data.txt;
#read {i in 1..M+1, j in 1..M+1} W[i,j] < ("output/W_robot_sigma=" & sigma & ".out");
#read b < ("output/b_robot_sigma=" & sigma & ".out");
