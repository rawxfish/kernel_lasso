let M := 30;
let N := 569;
let nzeros := 1;
let sigma := 2;
read {j in 1..N} Y[j] < WI_breast_cancer_label.txt;
read {i in 1..M, j in 1..N} X[i,j] < WI_breast_cancer_data.txt;
#read {i in 1..M+1, j in 1..M+1} W[i,j] < ("output/W_WI_sigma=" & sigma & ".out");
#read b < ("output/b_WI_sigma=" & sigma & ".out");
